{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "format-resources",
            "type": "shell",
            "command": "make format-resources",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}